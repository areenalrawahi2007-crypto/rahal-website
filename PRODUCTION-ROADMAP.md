# خطة تحويل موقع رحّال من ديمو إلى تطبيق حقيقي

هذا الملف يلخّص كل قطعة ناقصة بين الوضع الحالي (موقع ثابت، كل البيانات محلية بمتصفح الزائر) وبين تطبيق حقيقي يستخدمه عملاء وموردون فعليون. مرجع سريع لك أو لأي مطوّر تستعين فيه لاحقًا.

## 1. الوضع الحالي بالضبط
- الموقع HTML/CSS/JS ثابت بالكامل، بدون خادم (Backend) وبدون قاعدة بيانات.
- المنتجات والرحلات والموردين مكتوبين يدويًا في `data.js` — أي تعديل يحتاج فتح الكود.
- السلة تُحفظ في متصفح الزائر فقط (`localStorage`) — لو غيّر جهاز أو مسح بيانات المتصفح، تضيع.
- نموذج "انضم كمورد" ونموذج "تواصل معنا" يعرضان رسالة نجاح، لكن **البيانات ما تُحفظ ولا توصلك فعليًا** — تختفي بمجرد إغلاق الصفحة.
- صفحة الدفع (فاتورة الشراء وفاتورة الإيجار) وهمية بالكامل — ما فيه بوابة دفع فعلية، ولا حجز فعلي يوصل لأي مورد.
- ما فيه تسجيل دخول لا للعميل ولا للمورد — كل زائر "مجهول".

## 2. الفجوات الأساسية (مرتبة حسب الأولوية)

| الفجوة | التأثير لو ما اتحلّت | الحل المقترح |
|---|---|---|
| لا يوجد قاعدة بيانات حقيقية | الطلبات وتسجيلات الموردين تضيع، ما توصلك | قاعدة بيانات (Supabase مقترح) |
| لا يوجد تسجيل دخول | العميل ما يشوف طلباته السابقة، المورد ما عنده حساب خاص | نظام مصادقة (Auth) |
| لا يوجد بوابة دفع حقيقية | ما فيه دخل فعلي، الدفع شكلي بس | ربط بوابة دفع (Thawani / PayTabs مناسبة لعُمان) |
| لا يوجد رفع ملفات حقيقي | وثائق ترخيص/تأمين المورد ما تُحفظ | تخزين ملفات (Supabase Storage) |
| لا يوجد إشعارات | العميل والمورد ما يعرفون بحالة الطلب | إيميل أو واتساب عند كل حجز |
| لا توجد لوحة إدارة | لازم تعدّل `data.js` يدويًا لكل منتج/مورد جديد | لوحة إدارة بسيطة (تُبنى فوق قاعدة البيانات) |

## 3. المسار المقترح: Supabase (الأسرع والأنسب لغير المبرمج)

[Supabase](https://supabase.com) خدمة مجانية (للبداية) توفر:
- قاعدة بيانات حقيقية (PostgreSQL) بدون ما تبني خادم من الصفر.
- نظام تسجيل دخول جاهز (بريد إلكتروني/كلمة مرور أو رقم جوال).
- تخزين ملفات (لوثائق الموردين).
- تتصل مباشرة من نفس `app.js`/`data.js` الحاليين بأسطر كود بسيطة — **ما نرمي الموقع الحالي ونبدأ من جديد**، فقط نستبدل `localStorage` والبيانات الثابتة تدريجيًا ببيانات حقيقية من Supabase.

### خطوات المسار (عند البدء الفعلي):
1. إنشاء مشروع Supabase مجاني وربطه بالموقع.
2. نقل `PRODUCTS` و`TRIPS` و`SUPPLIERS` من `data.js` إلى جداول حقيقية في قاعدة البيانات.
3. بناء نظام تسجيل دخول بسيط للعميل وللمورد.
4. ربط نموذج "انضم كمورد" و"تواصل معنا" بحفظ حقيقي في قاعدة البيانات (بدل ما يختفي).
5. ربط صفحتي فاتورة الشراء وفاتورة الإيجار ببوابة دفع حقيقية.
6. بناء لوحة إدارة بسيطة (حتى لو صفحة واحدة محمية بكلمة مرور) لإدارة المنتجات والطلبات بدل تعديل الكود يدويًا.
7. إشعارات إيميل أو واتساب عند كل حجز جديد.

## 4. نقاط تحتاج قرار منك قبل الإطلاق الفعلي
- اسم الشركة القانوني والسجل التجاري (مطلوب لبوابة الدفع ولصياغة الشروط والأحكام رسميًا).
- مراجعة قانونية لصفحة `terms.html` (خصوصًا بند إخلاء المسؤولية عن تأجير المركبات) من محامٍ مختص قبل الاعتماد الرسمي.
- نسب العمولة اللي راح تاخذها رحّال من كل حجز/بيع (غير محددة حاليًا بالكود).
- طريقة الدفع الفعلية للموردين (تحويل بنكي؟ كل أسبوع؟ كل شهر؟).

## 5. ما لا يحتاج تغيير
- التصميم والهوية البصرية (الألوان، الخطوط، شكل الصفحات) — جاهزة وما تحتاج إعادة بناء.
- بنية الصفحات المنفصلة (رحلات، معدات، مركبات، تموين، سلة، دفع) — منطقية وتصلح كأساس للتطبيق الحقيقي.
- منطق فصل فاتورة الشراء عن فاتورة الإيجار — نفس المنطق ينطبق لاحقًا مع بوابة دفع حقيقية.
